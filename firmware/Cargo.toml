[package]
name = "firmware"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
cortex-m-rt = "0.7.5"
rp2040-boot2 = "0.3"
# rp2040-hal = { version = "0.11", features = ["rt", "critical-section-impl"] }
